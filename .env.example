# Copy this file to .env and adjust. .env is gitignored and never committed.

# --- PostgreSQL (used by docker/compose.yaml and by the app) ---
POSTGRES_USER=ai_news
POSTGRES_PASSWORD=change-me-locally
POSTGRES_DB=ai_news
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# --- Ollama (local LLM) ---
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=qwen3:8b

# --- Email (filled in later) ---
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM=
EMAIL_TO=
